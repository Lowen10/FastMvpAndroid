<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!--<item name="android:buttonStyle">@style/BtnStyle</item>-->
        <item name="buttonStyle">@style/BtnStyle</item>
        <item name="android:editTextStyle">@style/EdtStyle</item>
        <item name="android:textViewStyle">@style/TextStyle</item>

        <item name="android:windowBackground">@color/backgroundColor</item>
    </style>

    <style name="BtnStyle">
        <item name="android:background">@drawable/btn_background_drawable</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">10dp</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="EdtStyle">
        <item name="android:textColorHint">#FFFAFA</item>
        <item name="android:textSize">12sp</item>
        <item name="android:textCursorDrawable">@null</item>
        <item name="android:drawablePadding">10dp</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:background">@drawable/edittext_background_drawable</item>
    </style>

    <style name="TextStyle">
        <item name="android:textAllCaps">false</item>
    </style>

</resources>
